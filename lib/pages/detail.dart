import 'package:flutter/material.dart';

class Detail extends StatelessWidget {
  final int countryId;

  const Detail({required this.countryId, super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}