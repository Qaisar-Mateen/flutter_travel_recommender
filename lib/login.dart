import "package:flutter/material.dart";


class Login extends StatelessWidget {
  
  const Login({super.key});
  
  @override
  Widget build(BuildContext context) {
    return  const Scaffold(
      drawer: Icon(Icons.menu_outlined),
      body: Center(
        child: Card(
          child: Text("WHYYYYYY"),
        ),
      ),
    );
  }

}